// Header.js

import React from 'react';
import './Header.css'; // Import the corresponding CSS file

const Header = () => {
  return (
    <header className="app-header">
      <h1>React Queue App</h1>
    </header>
  );
}

export default Header;
